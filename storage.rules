rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Evidencias técnicas subidas desde st/dash (Firebase Auth = usuarios del inventario aprobados).
    match /st_tecnico/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
